apiVersion: v1
kind: Service
metadata:
  labels:
    app: ovia-deploy
  name: ovia-svc
  namespace: devops
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: ovia-deploy
  type: NodePort
